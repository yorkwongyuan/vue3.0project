const path = require("path");

// const resolve = (...file) => path.resolve(__dirname, ...file);

// console.log(resolve("../src/components",'hehe')) // 无论路径中的文件是否存在

const res = path.dirname('/index/lw/td')
console.log(res)